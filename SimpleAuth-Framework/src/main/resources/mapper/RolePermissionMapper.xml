<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.eu.yaesakura.simpleauth.framework.mapper.RolePermissionMapper">

    <!--  根据角色ID列表查询角色权限关系  -->
    <select id="getRolePermissionsByRoleIds" resultType="org.eu.yaesakura.simpleauth.framework.domain.entity.RolePermission">
        SELECT *
        FROM role_permission
        WHERE role_id
        IN
        <foreach collection="roleIdList" item="roleId" separator="," open="(" close=")">
            #{roleId}
        </foreach>
    </select>

</mapper>